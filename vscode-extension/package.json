{
  "name": "markasset",
  "displayName": "MarkAsset",
  "repository": {
    "type": "git",
    "url": "git://github.com/username/repository.git"
  },
  "description": "Streamline adding images to markdown documents with upload codes",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "icon": "icons/icon.png",
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "markasset.startSession",
        "title": "Start Upload Session",
        "category": "MarkAsset"
      },
      {
        "command": "markasset.logout",
        "title": "Logout from Google Drive",
        "category": "MarkAsset"
      }
    ],
    "configuration": {
      "title": "MarkAsset",
      "properties": {
        "markasset.googleClientId": {
          "type": "string",
          "default": "",
          "description": "Google OAuth 2.0 Client ID (Desktop app type)"
        },
        "markasset.googleClientSecret": {
          "type": "string",
          "default": "",
          "description": "Google OAuth 2.0 Client Secret (Desktop app type — not truly secret, required by Google's API)"
        },
        "markasset.pollInterval": {
          "type": "number",
          "default": 2000,
          "description": "Poll interval in milliseconds for checking uploaded files"
        },
        "markasset.downloadPaths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Custom download folder paths. Supports `${workspaceFolder}` and `${fileDirname}` variables. When set, replaces the default paths (`${workspaceFolder}/images` and `${fileDirname}/images`). Paths must be absolute (use variables, not relative paths like `./images`)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "@types/vscode": "^1.74.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "google-auth-library": "^10.5.0",
    "googleapis": "^105.0.0"
  }
}
